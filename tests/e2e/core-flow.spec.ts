import { describe, it } from 'vitest';

describe('core flow placeholders', () => {
  it('defines macOS e2e scenarios for manual/CI extension', () => {
    // Placeholder for hotkey -> capture -> OCR -> clipboard integration testing.
  });
});
